{
    "size" : 1000,

    "query": { "bool": {
      <% tags.each_with_index do | tag, index | %>
        "must": { "term": {  "__tags" : "<%= tag %>"  }},
      <% end %>

      "must_not": { "term": { "__tags" : "backendonly" }}

    }}
}