#!GET /callowayart/art/_search
{
  "size" : 10,

  "query": { "bool": {
    "must_not": { "term": { "__tags" : "backendonly" }}
  }},

  "sort" : {
    "_script" : { 
        "script" : "Math.random()",
        "type" : "number",
        "params" : {},
        "order" : "asc"
    }
  }
}
