{
    "size" : 1,

    "query": { "bool": {

    	<% if artist_slug %>
        "must": { "term": { "artist_slug" : "<%= artist_slug %>" }},
      <% end %>

        "must": { "term": { "title_slug"  : "<%= title_slug %>"  }}

    }}
}